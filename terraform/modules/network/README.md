# Network Module

Creates the deployed two-AZ VPC foundation: public and private subnets, an
Internet Gateway, one NAT Gateway, and separate public and private route tables.
The single NAT Gateway is a deliberate portfolio cost and availability trade-off.
